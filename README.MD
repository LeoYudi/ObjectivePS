# Processo seletivo Objective

## Como executar
- Utilize o comando `yarn install` para instalar todas as dependências
- Utilize o comando `yarn dev` para iniciar o projeto 
- Acesse o link http://localhost:3333/game